name: Daily Export Issues CSV

on:
  schedule:
    - cron: "0 6 * * *"
  workflow_dispatch:

jobs:
  export:
    runs-on: ubuntu-latest

    permissions:
      contents: read
      issues: read

    steps:
      - name: Fetch Issues
        run: |
          curl -L \
               -H "Accept: application/vnd.github+json" \
               -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
               https://api.github.com/repos/${{ github.repository }}/issues?state=all \
               -o issues.json

      - name: Install jq
        run: |
          sudo apt-get update
          sudo apt-get install -y jq

      - name: Convert JSON to CSV
        run: |
          echo "number,title,state,created_at" > issues.csv
          jq -r '.[] | select(.pull_request|not) | [.number, .title, .state, .created_at] | @csv' issues.json >> issues.csv

      - name: Upload CSV File
        uses: actions/upload-artifact@v4
        with:
          name: daily-issues-export
          path: issues.csv